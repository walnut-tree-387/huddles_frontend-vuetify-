export const aliases: Record<string, any> = {}

const defaultExpansionPanels = {
  variant: 'default',
  VExpansionPanel: {
    elevation: 0
  }
}
export const defaults = {
  VExpansionPanels: { ...defaultExpansionPanels }
}
